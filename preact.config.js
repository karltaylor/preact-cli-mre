export default (config, env, helpers) => {
  /**
   * Uncomment below to fix build error.
   */
  //   config.resolve.alias = {
  //     ...config.resolve.alias,
  //     react: "preact/compat",
  //     "react-dom": "preact/compat",
  //   };

  console.log(config);
};
